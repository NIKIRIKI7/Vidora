# --- API-каркас воркера ---
fastapi>=0.100.0
uvicorn[standard]>=0.23.0
pydantic>=2.0.0

# --- ML-ядро (общее) ---
numpy>=1.24.0
soundfile>=0.12.0

# --- OmniVoice GGUF (основной режим, CrispASR / ggml) ---
# Веса: cstr/omnivoice-GGUF (omnivoice-q8_0.gguf + omnivoice-tokenizer-f16.gguf)
# кладутся в <TTS_MODELS_DIR>/OmniVoice-GGUF (см. adapters/omni_voice_adapter.py).
# CPU-колесо:
#   pip install crispasr
# CUDA-колесо (Windows/Linux, NVIDIA):
#   pip install crispasr --extra-index-url https://crispstrobe.github.io/CrispASR/whl/cuda/
crispasr>=0.8.32

# --- OmniVoice PyTorch (legacy-режим, опционально) ---
# Нужен только если GGUF-файлы не найдены. Модель: k2-fsa/OmniVoice.
# pip install torch torchaudio omnivoice
#
# Для int8-квантования дополнительно (опционально):
# pip install bitsandbytes accelerate
