export const SCENARIO_PARSER_RULES = `# СТРОГИЕ ПРАВИЛА ПАРСИНГА СЦЕНАРИЯ В VIDORA
Движок Vidora строго парсит Markdown-файл сценария. Выдай текст ТОЛЬКО в этом формате, без пояснений.

## 1. YAML Frontmatter
В самом начале файла между \`---\`:
- \`title\`: "Заголовок" (в кавычках)
- \`fps\`: 30
- \`primary\`, \`secondary\`, \`background\`, \`surface\`, \`accent\`, \`text\`: цвета в "#HEX"

## 2. Сцены
Каждая сцена начинается с заголовка в квадратных скобках и таймкода в круглых:
\`[Название сцены] (00:00:00)\`

## 3. Фрагменты
Каждая строка под заголовком сцены — это фрагмент (смена плана).
Синтаксис: \`*(Визуальная ремарка)* Текст озвучки.\`
- Ремарка в \`*()*\` описывает визуал на экране (Экран, B-roll, Анимация, Схема).
- Текст после скобок — слова диктора. Если текста нет — чистый B-roll без голоса.
- ВАЖНО: В начале каждой ремарки указывай один из визуальных архетипов:
  - \`Постерный сплит\` — контрастный геометрический фон, гигантская типографика, объект-вырезка.
  - \`Журнальный минимализм\` — чистый светлый/тёмный фон, один предмет-метафора, крупный заголовок.
  - \`Кислотный коллаж\` — наклонные цветные ленты, стикеры-звёзды, ретро-гранж.
  - \`Неоновый хай-тек\` — тёмный фон, светящиеся неоновые элементы, голограммы.
  - \`Инфографика\` — карты, графики, огромные счётчики цифр.
  Пример: \`*(Постерный сплит: огромный текст «SPOTLIGHT», сзади чёрно-белый круг, в центре кроссовок)*\`

## 4. Управление голосом (Voice Tags)
Внутри текста озвучки используй специальные теги для интонации (они не произносятся вслух).
{{VOICE_RULES}}
`
