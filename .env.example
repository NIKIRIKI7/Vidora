# Vidora environment variables
# Copy this file to frontend/.env — Vite читает переменные из каталога frontend.

# Базовый URL backend2 (REST + WebSocket), который использует фронтенд.
# Если не задан — http://localhost:5116
VITE_API_URL=http://localhost:5116
